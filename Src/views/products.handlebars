<nav class="navContainer">
    <ul class="nav-ul">

        </div>
            <li><a href="/home" class="nav-item">HOME</a></li>
        <div>
    
         <li><a href="/products/?category=placasdevide" class="nav-item">Placas de video</a></li>
         <li><a href="/products/?category=procesadores" class="nav-item">Procesadores</a></li>
         <li><a href="/products/?category=ram" class="nav-item" >Memorias Ram</a></li>
    </ul>
</nav>


<div class="bodyContainer">
<div class="productsContainer">
    {{#each products}}
  <div class="productCard">
        <img src={{this.img}} alt={{this.description}}>
        <p>Title:{{this.title}}</p>
        <p>Description: {{this.description}} </p>
        <p>Category:{{this.category}} </p>
        <p>Price:{{this.price}}</p>

        {{#if ../isUserOrPremium}}
        <button id="btn-subtract-product" class="btn-subtract-product">-</button>
        <span id="spam-quantity" class="spam-quantity">1</span>
        <button id="btn-add-more-product" class="btn-add-more-product"  data-stock="{{this.stock}}">+</button>
        <button class="btnCard"  data-product-id="{{this._id}}" >Add to cart</button>
        <button class="btnDeleteProduct btnUpdateAndDelete"  data-product-id="{{this._id}}">Eliminar producto</button>
        {{/if}}

    </div>
    {{/each}}
</div>




    {{#if hasPrevPage}}
    <div class="btnsContainer"></div>
    <a href="/products/?page={{prevPage}}"><button> < Anterior</button> </a>
    {{/if}}

    <div class="numberPage">
   {{page}}
    </div>

    {{#if hasNextPage}}
    <a href="/products/?page={{nextPage}}"><button>Próxima > </button> </a>
    </div>
    {{/if}}


{{#if productsfilter}}
<div class="productsContainer"> 
{{#each productsfilter}}
  <div class="productCard">
       <img src={{this.img}} alt={{this.description}}>
        <p>Title:{{this.title}}</p>
        <p>Description: {{this.description}} </p>
        <p>Category:{{this.category}} </p>
        <p>Price:{{this.price}}</p>

        {{#if ../isUserOrPremium}}
        <button id="btn-subtract-product" class="btn-subtract-product">-</button>
        <span id="spam-quantity" class="spam-quantity">1</span>
        <button id="btn-add-more-product" class="btn-add-more-product"  data-stock="{{this.stock}}">+</button>
        <button class="btnCard"  data-product-id="{{this._id}}" >Add to cart</button>
        <button class="btnDeleteProduc btnUpdateAndDelete"  data-product-id="{{this._id}}" >Eliminar producto</button>
        {{/if}}
       

    </div>
{{/each}}
</div>
{{/if}}

</div>

 <script src="../public//js/products.js"></script> 




